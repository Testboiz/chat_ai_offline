name: chat_ai_offline
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0
  shared_preferences: ^2.5.3
  shared_preferences_android: ^2.4.11
  sqflite: ^2.4.1
  timeago: ^3.7.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/db/chat_database.db
  uses-material-design: true
